package hash

// todo

/*
func zTest_Set(t *testing.T) {
	s := NewUintHashSet()
	kg := newKeygen(unlim)
	for i := uint64(0); i < N; i++ {
		s.Add(uint(kg.Next()))
	}
	kg = newKeygen(unlim)
	for i := uint64(0); i < N; i++ {
		assert.True(t, s.Includes(uint(kg.Next())))
	}
}
*/
